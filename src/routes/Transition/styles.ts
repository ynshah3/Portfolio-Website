import { makeStyles } from "@material-ui/styles";

export const styles = makeStyles({
  grid: {
    height: '100vh',
    backgroundColor: '#DFBD69'
  },
  logoContainer: {
    borderRadius: '50%',
    backgroundColor: '#FFFFFF',
    width: 100,
    height: 100,
    margin: 'calc(50vh - 55px) calc(50vw - 55px)',
    position: 'absolute',
    alignItems: 'center',
    padding: 10
  }
});
