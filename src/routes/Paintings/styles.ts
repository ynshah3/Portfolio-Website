import { makeStyles } from "@material-ui/styles";

export const styles = makeStyles({
  marginBottom50: {
    marginBottom: 50
  },
  verticalCenterify: {
    display: 'flex',
    justifyContent: 'center'
  }
});